import { Network } from 'lucide-react';
import FeaturePreview from '@/components/FeaturePreview';

export default function NetworkPage() {
  return <FeaturePreview icon={Network} eyebrow="Network intelligence" title="Traffic, without blind spots." description="Flow analytics, packet loss and interface health are being shaped into one focused network workspace." />;
}
